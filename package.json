{
  "name": "canvas-ts-project",
  "version": "0.1.0",
  "description": "Проект Canvas з кружечками, стрілками та видаленням на TypeScript",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && live-server --open=src/index.html --port=8080 --watch=./",
    "format": "prettier --write \"src/**/*.{js,jsx,ts,tsx,json,css,scss,md}\""
  },
  "keywords": [
    "canvas",
    "typescript",
    "drawing",
    "deletion"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2",
    "typescript": "^5.0.0"
  }
}
